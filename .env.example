# ===========================================
# TenderTalks Environment Variables
# ===========================================

# ===== CLIENT-SIDE (VITE_ prefix = exposed to browser) =====
# These are bundled into the frontend by Vite

# Supabase - Get from: https://supabase.com/dashboard/project/_/settings/api
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Razorpay - Get from: https://dashboard.razorpay.com/app/keys
VITE_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxxx

# App - Primary domain is tendertalks.live
VITE_APP_URL=https://tendertalks.live
VITE_APP_NAME=TenderTalks

# ===== SERVER-SIDE (Vercel API routes) =====
# These are only available in serverless functions

# Supabase (same URL as client, but without VITE_ prefix for server access)
SUPABASE_URL=https://your-project.supabase.co
# Service role key - Get from Supabase Dashboard > Settings > API > service_role (secret)
# NEVER expose this to the client!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Neon Database - Get from: https://neon.tech
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Razorpay (server-side)
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxxx
RAZORPAY_KEY_SECRET=your-secret-key

# Webhook secret - Get from Razorpay Dashboard > Webhooks > Create Webhook
# Set webhook URL to: https://tendertalks.live/api/payments/webhook
# Enable these webhook events:
#   - payment.authorized, payment.captured, payment.failed
#   - refund.created, refund.processed, refund.failed
#   - subscription.activated, subscription.charged, subscription.cancelled, subscription.halted
#   - invoice.paid, invoice.expired
#   - order.paid
RAZORPAY_WEBHOOK_SECRET=your-webhook-secret

# ===========================================
# Domain Configuration
# ===========================================
# Primary domain: tendertalks.live
# www redirect: www.tendertalks.live -> tendertalks.live
# Vercel fallback: tendertalks.vercel.app
#
# In Vercel Dashboard > Settings > Domains:
# 1. Add tendertalks.live as primary
# 2. Add www.tendertalks.live with redirect to tendertalks.live
# 3. Keep tendertalks.vercel.app as fallback

# ===========================================
# Vercel Deployment
# ===========================================
# Add ALL of these to Vercel Dashboard > Settings > Environment Variables:
#
# CLIENT-SIDE (needed for frontend):
#   - VITE_SUPABASE_URL
#   - VITE_SUPABASE_ANON_KEY
#   - VITE_RAZORPAY_KEY_ID
#   - VITE_APP_URL (set to https://tendertalks.live)
#   - VITE_APP_NAME
#
# SERVER-SIDE (needed for API routes):
#   - SUPABASE_URL
#   - SUPABASE_SERVICE_ROLE_KEY
#   - DATABASE_URL
#   - RAZORPAY_KEY_ID
#   - RAZORPAY_KEY_SECRET
#   - RAZORPAY_WEBHOOK_SECRET
#
# ===========================================
# Razorpay Webhook Setup
# ===========================================
# 1. Go to Razorpay Dashboard > Webhooks
# 2. Create new webhook with URL: https://tendertalks.live/api/payments/webhook
# 3. Select these events:
#    - payment.authorized
#    - payment.captured
#    - payment.failed
#    - refund.created
#    - refund.processed
#    - refund.failed
#    - subscription.activated
#    - subscription.charged
#    - subscription.cancelled
#    - subscription.halted
#    - subscription.pending
#    - subscription.completed
#    - invoice.paid
#    - invoice.expired
#    - order.paid
# 4. Copy the webhook secret and add to RAZORPAY_WEBHOOK_SECRET
